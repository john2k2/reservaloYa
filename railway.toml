[build]
dockerfilePath = "docker/pocketbase/Dockerfile"
buildCommand = ""

[deploy]
startCommand = "./pocketbase serve --http=0.0.0.0:8090"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
